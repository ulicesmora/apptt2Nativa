import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appsos.dev',
  appName: 'app-movil-sos',
  webDir: 'dist/app-movil-tt2/browser'
};

export default config;
