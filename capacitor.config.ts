import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sossismo.dev',
  appName: 'app-movil-tt2',
  webDir: 'dist/app-movil-tt2/browser'
};

export default config;
